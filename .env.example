# Node Environment
# Options: development, production, test
NODE_ENV=development

# Database Configuration (Optional - app works without database)
# If not provided, the app will fall back to JSON data files
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_database_name
DB_PORT=3306
# Enable SSL for database connection (true/false)
# In production, SSL should be enabled and certificates should be validated
DB_SSL=false

# Note: Never commit actual .env files to version control
# This file serves as a template for required environment variables
